import 'package:flutter/material.dart';

abstract class AppStyles{
  //Text
  static const textStyle = TextStyle(fontSize: 15, color: Colors.black);
  static const paddingNote = EdgeInsets.only(left: 10, top: 5, bottom: 5);
}